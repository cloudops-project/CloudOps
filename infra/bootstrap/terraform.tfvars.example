aws_region        = "us-east-1"
state_bucket_name = "replace-with-globally-unique-cloudops-state-bucket"
lock_table_name   = "cloudops-terraform-locks"
github_repository = "replace-owner/replace-repository"

tags = {
  Owner       = "platform-team"
  Environment = "shared"
}
